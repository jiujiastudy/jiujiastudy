# STCanvas 使用说明

连上你自己的 Canvas，帮你盯两件事：什么时候要交什么、这周每门课该学什么。会根据你的进度和你说的话判断状态，状态不好时给建议。支持 Claude Code、Codex 等能跑命令的 AI 助手，Windows 和 Mac 都行。

## 装法
把 `stcanvas` 文件夹整个放进 `C:\Users\你的用户名\.claude\skills\`（Mac：`~/.claude/skills/`；Codex：`~/.codex/skills/`）。也可以把 `STCanvas.zip` 交给 Codex 或 Claude Code，说「安装 STCanvas skill」，让它解压到对应的 skills 文件夹。装好后新开一个对话即可使用。

## 第一次
新开一个对话，随便说一句和课有关的话，比如「最近要交什么」。它只要两样东西：
1. Canvas 网址：它先在你浏览器记录里找（只看域名），找到会告诉你，找不到才要你发。
2. token：去 Canvas → Account → Settings → Approved Integrations → New Access Token，复制后粘进它弹出的窗口（不要贴进对话），然后再随便回一句。
网络和权限已经就绪时，通常一分钟内出 deadline 雷达和本周学习清单；首次权限批准或依赖准备的时间另算。课件随后由单个后台任务自动下载。

## 东西在哪
桌面的「STCanvas」文件夹：根目录是「STCanvas.html」和「Deadline雷达.html」，每门课一个文件夹，里面「课件」是 Canvas 上的原件，「产出」是它帮你做的东西。想放别处，说「资料放到 D 盘」之类就行。

## 平时这样说
「现在什么情况」「最近要交什么」「这周学什么」「做完了」「没状态」，或者直接说你想做的事：导读课件、做复习包、写东西、发帖、交作业。

## 要知道
- 它看不到 Canvas 以外的东西（课表、邮箱、Padlet），会列成「待确认」让你去查。
- 学校对 AI 的规定各不相同，用之前自己看一眼这门课的要求。
- token 等于你的 Canvas 账号：别给别人，学期结束去 Canvas 删掉。
